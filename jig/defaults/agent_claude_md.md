# jig agents — global CLAUDE.md

This file is loaded into every jig-spawned agent's context via
`$CLAUDE_CONFIG_DIR/CLAUDE.md`. Real content lands in a follow-up PR;
this placeholder exists so the injection mechanism has something to copy.
