# todo_cli

A small command-line todo manager. Three subcommands (`add`, `list`, `done`), file-backed persistence in the
working directory, standard library only.

This file is a reference for prompt authors and reviewers — it is **never** sent to the model. The model sees only
the prompt under `prompts/<style>.md`.
